[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kidsql"
version = "0.1.0"
description = "SQL made so easy a kindergartner can use it."
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["kidsql*"]
